name: Benchmark

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "0 6 * * 1"

permissions:
  contents: write
  pull-requests: write

jobs:
  benchmark:
    name: Run benchmarks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"
          python -m pip install pytest-benchmark

      - name: Run benchmarks
        run: |
          pytest tests/benchmark/ --benchmark-json=benchmark-results.json --benchmark-save=latest -q 2>&1 || true
          mkdir -p benchmarks
          # Check if any benchmarks were collected
          if grep -q '"benchmarks": *\[\]' benchmark-results.json 2>/dev/null; then
            echo "No benchmarks found, skipping upload"
            echo "has_benchmarks=false" >> $GITHUB_OUTPUT
          else
            echo "has_benchmarks=true" >> $GITHUB_OUTPUT
          fi
        id: benchmark_check

      - name: Store benchmark results
        if: github.event_name == 'push' && github.ref == 'refs/heads/main' && steps.benchmark_check.outputs.has_benchmarks == 'true'
        uses: benchmark-action/github-action-benchmark@v1
        with:
          name: SonoForge Benchmarks
          tool: 'pytest'
          output-file-path: benchmark-results.json
          auto-push: true
          github-token: ${{ secrets.GITHUB_TOKEN }}
          benchmark-data-dir-path: benchmarks
          alert-threshold: '150%'
          fail-on-alert: false
          summary-always: true

      - name: Comment on PR
        if: github.event_name == 'pull_request' && steps.benchmark_check.outputs.has_benchmarks == 'true'
        uses: benchmark-action/github-action-benchmark@v1
        with:
          name: SonoForge Benchmarks
          tool: 'pytest'
          output-file-path: benchmark-results.json
          auto-push: false
          github-token: ${{ secrets.GITHUB_TOKEN }}
          alert-threshold: '150%'
          fail-on-alert: false
          summary-always: true
          comment-on-alert: true
