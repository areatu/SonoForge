# SonoForge Presenter — build/runtime dependency set (lite profile).
#
# Same packages as the full pyproject.toml MINUS:
#   onnxruntime  (AI segmentation — not in Presenter)
#   pymupdf      (reference PDF rendering — not in Presenter)
#   openpyxl     (reference constructor Excel import — not in Presenter)
# and with PySide6-Essentials instead of the full PySide6 metapackage
# (skips PySide6-Addons → no QtWebEngine, ~175 MB wheel saved).
#
# Versions are PINNED to the exact resolutions in uv.lock so the Presenter
# build is reproducible and the dependency-review CI gate sees no "new"
# dependency versions versus the base graph. When uv.lock is refreshed,
# update these pins deliberately (script: see build/presenter/README.md).
#
# Usage (build machine):
#   pip install -r build/presenter/requirements-presenter.txt pyinstaller
#   python -m PyInstaller build/presenter/sonoforge-presenter.spec --noconfirm --clean

pyside6-essentials==6.11.1
pyqtgraph==0.14.0
pydicom==3.0.2
pylibjpeg==2.1.0
pylibjpeg-openjpeg==2.3.0
pylibjpeg-libjpeg==2.2.0
numpy==1.26.4
scipy==1.15.3
opencv-python-headless==4.11.0.86
httpx==0.28.1
psutil==7.2.2
pynetdicom==3.0.4
pyyaml==6.0.3
jsonschema==4.26.0
reportlab==4.5.1
keyring==25.7.0
